export enum STATUS {
	IN_LOBBY = "in_lobby",
	IN_PROGRESS = "in_progress",
	FINISHED = "finished",
}

export enum TYPE {
	TRUTH_OR_DARE = 1,
	WOULD_YOU_RATHER = 2,
}

export enum ACTION {
	WAITING_FOR_SELECTION = "waiting_for_selection",
	TRUTH = "truth",
	DARE = "dare",
}
